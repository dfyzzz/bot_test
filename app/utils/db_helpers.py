"""Helper functions for database operations in dialogs"""


async def get_user_by_telegram_id(session, telegram_id: int):
    """Get user by telegram ID - placeholder function"""
    # Функция будет реализована в рамках существующей системы запросов к базе данных
    # чтобы избежать дублирования кода и проблем с импортом
    return None


async def get_user_bookings(session, user_id: int):
    """Get bookings for a user - placeholder function"""
    # Функция будет реализована в рамках существующей системы запросов к базе данных
    # чтобы избежать дублирования кода и проблем с импортом
    return []