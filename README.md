# Telegram-бот "Секретарь для записи"

Telegram-бот для автоматизации записи клиентов к частному мастеру, реализованный с использованием aiogram 3.x, aiogram-dialogs и других современных технологий.

## Особенности

- 🤖 Интерактивный интерфейс с использованием диалогов
- 📅 Система бронирования с выбором даты и времени
- 👤 Личный кабинет пользователя с историей записей
- 💰 Программа лояльности с накоплением скидок
- 📊 Админ-панель для управления записями
- 📈 Экспорт данных в Excel
- ⏰ Система уведомлений и напоминаний
- 🔐 Защита админ-функций от несанкционированного доступа

## Технический стек

- Python 3.11+
- aiogram 3.x
- aiogram-dialogs
- PostgreSQL
- Redis
- APScheduler
- SQLAlchemy
- Docker

## Установка и запуск

1. Скопируйте репозиторий:
   ```bash
   git clone <url>
   cd bot_learn
   ```

2. Заполните `.env` файл:
   ```
   BOT_TOKEN=ваш_токен_бота
   DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost/secretary_bot
   REDIS_URL=redis://localhost:6379
   ADMIN_USER_ID=ваш_айди_администратора
   ```

3. Запустите приложение с помощью Docker:
   ```bash
   docker-compose up --build
   ```

## Функционал

### Для клиентов:
- Запись на услуги через интерактивные диалоги
- Просмотр и редактирование профиля
- Просмотр истории записей
- Оставление отзывов
- Получение уведомлений и напоминаний

### Для администраторов:
- Управление записями клиентов
- Просмотр статистики
- Настройка графика работы
- Массовая рассылка
- Экспорт данных

## Архитектура

- `app/dialogs/` - диалоги для взаимодействия с пользователями
- `app/handlers/` - обработчики команд и событий
- `app/keyboards/` - inline-клавиатуры
- `app/database/` - модели и запросы к базе данных
- `app/services/` - вспомогательные сервисы
- `app/middlewares/` - промежуточное ПО
- `app/states/` - FSM состояния

## Безопасность

- Админ-панель доступна только пользователям с правами администратора
- Проверка прав доступа к каждой админ-функции
- Защита от дублирования пользователей в базе данных
- Использование безопасного хранения токенов

## Автор

Kilo Code